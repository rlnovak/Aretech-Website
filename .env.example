# Copie para `.env` (ou `.env.production`). NÃO versione o `.env` real.
# URL pública do Worker de chat na Cloudflare, incluindo /api/chat.
VITE_CHAT_API_URL="https://aretech-chat.SEU-SUBDOMINIO.workers.dev/api/chat"
